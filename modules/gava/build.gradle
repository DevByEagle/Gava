apply plugin: "java"

java {
    withJavadocJar()
}

tasks.jar {
    archiveBaseName.set("Gava")
    archiveVersion.set(version)
    archiveClassifier.set("") // no classifier, so jar will be Gava-1.0.0.jar
}